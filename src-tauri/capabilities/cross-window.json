{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "cross-window",
  "description": "Lets every window emit/listen for the `data:changed` broadcast (so an edit in one window refreshes the others) and lets a pop-out window close itself. The app's own #[tauri::command]s need no ACL; only these core JS APIs do.",
  "windows": ["main", "plan-*"],
  "permissions": [
    "core:event:allow-emit",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-close"
  ]
}
